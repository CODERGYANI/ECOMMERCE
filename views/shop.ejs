<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta
    name="viewport"
    content="width=device-width, initial-scale=1, viewport-fit=cover"
  />
  <title>Shop</title>
  <style>
    :root {
      /* 4-color palette: 3 neutrals + 1 accent */
      --bg: #f8fafc;       /* neutral background */
      --surface: #ffffff;  /* neutral surface */
      --text: #0f172a;     /* neutral text */
      --accent: #2563eb;   /* brand/accent */
      --radius: 14px;
      --shadow-sm: 0 1px 2px rgba(15, 23, 42, 0.06);
      --shadow-md: 0 10px 30px rgba(15, 23, 42, 0.12);
      --border: 1px solid rgba(15, 23, 42, 0.06);
      --muted: rgba(15, 23, 42, 0.6); /* derived from --text, not a new color */
    }

    * {
      box-sizing: border-box;
    }

    html, body {
      height: 100%;
    }

    body {
      font-family: "Poppins", system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, sans-serif;
      background-color: var(--bg);
      color: var(--text);
      margin: 0;
      line-height: 1.55;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    a {
      color: inherit;
      text-decoration: none;
    }
    a:focus-visible, button:focus-visible {
      outline: 2px solid var(--accent);
      outline-offset: 2px;
      border-radius: 8px;
    }

    .container {
      max-width: 1200px;
      margin-inline: auto;
      padding-inline: 16px;
      padding-block: 20px;
    }

    header.site-header {
      position: sticky;
      top: 0;
      z-index: 10;
      background: var(--bg);
      border-bottom: var(--border);
      backdrop-filter: saturate(180%) blur(6px);
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 1px 0 rgba(15, 23, 42, 0.04);
    }
    .site-header-inner {
      display: flex;
      align-items: center;
      justify-content: space-between;
      min-height: 56px;
      padding-block: 8px;
      gap: 12px;
    }
    .brand {
      display: flex;
      align-items: center;
      gap: 10px;
    }
    .brand h1 {
      font-size: 20px;
      font-weight: 700;
      letter-spacing: 0.2px;
      margin: 0;
    }
    .brand .hint {
      font-size: 12px;
      color: var(--muted);
    }
    .brand .logo {
      width: 28px;
      height: 28px;
      border-radius: 8px;
      background: var(--accent);
      color: #fff;
      display: grid;
      place-items: center;
      font-weight: 800;
      font-size: 14px;
      line-height: 1;
      box-shadow: var(--shadow-sm);
    }

    .actions {
      display: flex;
      align-items: center;
      gap: 10px;
    }
    .action-btn {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      font-weight: 600;
      font-size: 14px;
      padding: 8px 12px;
      color: var(--text);
      background: var(--surface);
      border-radius: 10px;
      border: var(--border);
      box-shadow: var(--shadow-sm);
      transition: background-color 160ms ease, transform 160ms ease, border-color 160ms ease;
    }
    .action-btn:hover {
      background: rgba(15, 23, 42, 0.03);
      transform: translateY(-1px);
      border-color: rgba(15, 23, 42, 0.1);
    }

    .action-btn--primary {
      background: var(--accent);
      color: #fff;
      border-color: rgba(37, 99, 235, 0.9);
    }
    .action-btn--primary:hover {
      background: #1e4fd1; /* darker tone of --accent */
      border-color: rgba(37, 99, 235, 1);
    }

    .logout-btn {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      font-weight: 600;
      font-size: 14px;
      padding: 8px 12px;
      color: var(--accent);
      background: transparent;
      border-radius: 10px;
      border: 1px solid rgba(37, 99, 235, 0.25);
      box-shadow: var(--shadow-sm);
      transition: background-color 160ms ease, transform 160ms ease, border-color 160ms ease;
    }
    .logout-btn:hover {
      background: rgba(37, 99, 235, 0.08);
      transform: translateY(-1px);
      border-color: rgba(37, 99, 235, 0.4);
    }
    .logout-btn:active {
      transform: translateY(0);
    }

    main {
      padding-block: 20px 40px;
    }

    .section-head {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      margin-bottom: 16px;
    }
    .section-head h2 {
      margin: 0;
      font-size: 18px;
      font-weight: 700;
      letter-spacing: 0.2px;
    }
    .section-sub {
      font-size: 13px;
      color: var(--muted);
    }

    .filter-bar {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      align-items: center;
      background: var(--surface);
      border: var(--border);
      border-radius: var(--radius);
      padding: 10px;
      margin-bottom: 16px;
      box-shadow: var(--shadow-sm);
    }
    .filter-input, .filter-number, .filter-select {
      appearance: none;
      font: inherit;
      color: var(--text);
      background: #fff;
      border: var(--border);
      border-radius: 10px;
      padding: 8px 10px;
      min-width: 0;
      box-shadow: var(--shadow-sm);
      transition: border-color 160ms ease, box-shadow 160ms ease;
    }
    .filter-input:focus-visible,
    .filter-number:focus-visible,
    .filter-select:focus-visible {
      outline: 2px solid var(--accent);
      outline-offset: 2px;
    }
    .filter-input {
      flex: 1 1 220px;
    }
    .filter-inline {
      display: flex;
      gap: 8px;
    }
    .filter-number {
      width: 110px;
    }
    .filter-select {
      width: 170px;
      background-image: none;
    }
    .filter-reset {
      border: 1px solid rgba(37, 99, 235, 0.25);
      color: var(--accent);
      background: transparent;
      border-radius: 10px;
      padding: 8px 12px;
      font-weight: 600;
      box-shadow: var(--shadow-sm);
      transition: background-color 160ms ease, transform 160ms ease, border-color 160ms ease;
    }
    .filter-reset:hover {
      background: rgba(37, 99, 235, 0.08);
      transform: translateY(-1px);
      border-color: rgba(37, 99, 235, 0.4);
    }

    .products-wrap {
      background: transparent;
    }
    .products-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
      gap: 20px;
    }

    .product-card {
      background-color: var(--surface);
      border-radius: var(--radius);
      border: var(--border);
      overflow: hidden;
      display: flex;
      flex-direction: column;
      box-shadow: var(--shadow-sm);
      transition: transform 180ms ease, box-shadow 180ms ease, border-color 180ms ease;
      min-height: 100%;
    }
    .product-card:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow-md);
      border-color: rgba(15, 23, 42, 0.1);
    }
    .product-card:focus-within {
      outline: 2px solid var(--accent);
      outline-offset: 2px;
    }

    .product-image {
      width: 100%;
      display: block;
      background-color: rgba(15, 23, 42, 0.04);
      object-fit: cover;
      aspect-ratio: 4 / 3; /* keeps consistent card heights */
      transition: transform 180ms ease;
    }
    .product-image.fallback {
      display: grid;
      place-items: center;
      color: var(--muted);
      font-size: 14px;
      font-weight: 600;
      letter-spacing: 0.2px;
    }
    .product-card:hover .product-image {
      transform: scale(1.02);
    }

    .product-details {
      padding: 16px;
      display: flex;
      flex-direction: column;
      gap: 12px;
      flex-grow: 1;
    }

    .product-name {
      font-size: 17px;
      font-weight: 700;
      margin: 0;
      letter-spacing: 0.2px;
      color: var(--text);
    }

    .product-style {
      font-size: 13px;
      color: var(--muted);
      margin-top: 4px;
    }

    .price-row {
      margin-top: auto;
      display: flex;
      align-items: baseline;
      gap: 10px;
    }
    .price {
      font-size: 16px;
      font-weight: 700;
      color: var(--accent);
      letter-spacing: 0.2px;
    }
    .discounted {
      font-size: 14px;
      color: var(--muted);
      text-decoration: line-through;
    }
    .price-row .badge {
      margin-left: 6px;
      padding: 2px 8px;
      font-size: 12px;
      font-weight: 700;
      color: #fff;
      background: var(--accent);
      border-radius: 999px;
      box-shadow: var(--shadow-sm);
      letter-spacing: 0.2px;
    }

    .empty {
      text-align: center;
      color: var(--muted);
      font-size: 14px;
      padding: 40px 0;
    }

    /* Responsive refinements */
    @media (min-width: 768px) {
      .brand h1 { font-size: 22px; }
      .section-head h2 { font-size: 20px; }
      .products-grid { gap: 24px; }
      .product-details { padding: 18px; }
      .product-name { font-size: 18px; }
      .price { font-size: 17px; }
      .filter-bar { gap: 12px; padding: 12px; }
      .filter-number { width: 130px; }
      .filter-select { width: 190px; }
    }
  </style>
</head>
<body>
  <header class="site-header">
    <div class="container site-header-inner">
      <div class="brand">
        <span class="logo" aria-hidden="true">S</span>
        <h1>Shop</h1>
        <span class="hint">Discover products</span>
      </div>
      <nav class="actions" aria-label="User actions">
        <a class="action-btn" href="/orders" aria-label="View my orders">My Orders</a>
        <a class="action-btn" href="/users/cart" aria-label="View my cart">My Cart</a>
        <a class="logout-btn" href="/users/logout" aria-label="Logout">Logout</a>
      </nav>
    </div>
  </header>

  <main>
    <div class="products-wrap">
      <div id="products-grid" class="products-grid">
        <% if (data && data.length > 0) { %>
          <% data.forEach(product => { %>
            <div
              class="product-card"
              style="background-color: <%= product.bgcolor || '#fff' %>;"
              data-name="<%= product.name %>"
              data-style="<%= product.styleinfo || '' %>"
              data-price="<%= product.price %>"
              data-effective-price="<%= product.discount ? (product.price - product.discount) : product.price %>"
            >
              <% if (product.image) { %>
                <img
                  class="product-image"
                  src="data:image/jpeg;base64,<%= product.image.toString('base64') %>"
                  alt="<%= product.name %>"
                  loading="lazy"
                  decoding="async"
                />
              <% } else { %>
                <div class="product-image fallback" role="img" aria-label="No image available">No Image</div>
              <% } %>

              <div class="product-details">
                <div>
                  <div class="product-name" style="color: <%= product.textcolor || 'var(--text)' %>;">
                    <%= product.name %>
                  </div>
                  <% if (product.styleinfo) { %>
                    <div class="product-style"><%= product.styleinfo %></div>
                  <% } %>
                </div>

                <div class="price-row">
                  <% if (product.discount) { %>
                    <span class="price"><%= product.price - product.discount %> ₹</span>
                    <span class="discounted"><%= product.price %> ₹</span>
                    <span class="badge">Save <%= product.discount %> ₹</span>
                  <% } else { %>
                    <span class="price"><%= product.price %> ₹</span>
                  <% } %>
                  <form action="/users/cart" method="POST" style="margin-left:auto;">
                    <input type="hidden" name="productId" value="<%= product._id %>">
                    <button type="submit" class="action-btn action-btn--primary" style="padding: 6px 10px; font-size: 13px;">Add to Cart</button>
                  </form>
                </div>
              </div>
            </div>
          <% }) %>
        <% } else { %>
          <p class="empty">No products available.</p>
        <% } %>
      </div>
    </div>
  </main>
</body>
</html>
