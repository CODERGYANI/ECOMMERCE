<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Premium Bags — Timeless Leather Craft</title>
    <meta name="description" content="Premium leather bags crafted for life. Sign up or log in to explore our collection." />

    <style>
      :root {
        /* Palette: 1 primary + 3 neutrals (total 4-5 colors) */
        --bg: #FAF7F2;        /* ivory background */
        --fg: #1A1A1A;        /* charcoal foreground */
        --muted: #6B6E73;     /* muted gray for secondary text */
        --accent: #C7A25A;    /* warm gold primary */
        --surface: #FFFFFF;   /* surface card */
        --radius: 0.625rem;   /* 10px rounding */
        --shadow-sm: 0 1px 2px rgba(0,0,0,0.06);
        --shadow-md: 0 8px 24px rgba(26,26,26,0.08);
      }

      /* Base reset */
      * { box-sizing: border-box; }
      html, body { height: 100%; }
      body {
        margin: 0;
        background: var(--bg);
        color: var(--fg);
        font-family: "Inter", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", sans-serif;
        line-height: 1.55;
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
      }
      h1, h2, h3 {
        margin: 0 0 0.5rem 0;
        font-family: "Playfair Display", ui-serif, Georgia, "Times New Roman", Times, serif;
        letter-spacing: 0.005em;
      }
      p { margin: 0; color: var(--muted); }

      a { color: inherit; text-decoration: none; }
      .sr-only {
        position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px;
        overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0;
      }

      /* Layout */
      .wrapper {
        min-height: 100dvh;
        display: grid;
        grid-template-rows: auto 1fr auto;
      }
      .container {
        width: 100%;
        max-width: 1120px;
        margin-inline: auto;
        padding: 1rem;
      }

      /* Header */
      header {
        position: sticky; top: 0; z-index: 10;
        background: rgba(250, 247, 242, 0.75);
        backdrop-filter: blur(6px);
        border-bottom: 1px solid rgba(0,0,0,0.05);
      }
      .nav {
        display: flex; align-items: center; justify-content: space-between;
        gap: 1rem;
      }
      .brand {
        display: inline-flex; align-items: center; gap: 0.5rem;
        font-weight: 600;
        letter-spacing: 0.08em;
      }
      .badge {
        display: inline-block;
        width: 12px; height: 12px; border-radius: 50%;
        background: var(--accent);
        box-shadow: 0 0 0 3px rgba(199,162,90,0.15);
      }
      .nav-actions {
        display: flex; align-items: center; gap: 0.5rem;
      }

      /* Buttons */
      .btn {
        display: inline-flex; align-items: center; justify-content: center;
        gap: 0.5rem;
        height: 44px; padding: 0 1rem;
        border-radius: var(--radius);
        border: 1px solid transparent;
        font-weight: 600; letter-spacing: 0.02em;
        transition: transform 140ms ease, background-color 140ms ease, color 140ms ease, border-color 140ms ease, box-shadow 140ms ease;
        cursor: pointer;
        box-shadow: var(--shadow-sm);
      }
      .btn:focus-visible {
        outline: 2px solid var(--accent);
        outline-offset: 2px;
      }
      .btn-primary {
        background: var(--accent);
        color: #111111; /* ensure strong contrast */
        box-shadow: 0 10px 22px rgba(199,162,90,0.22);
      }
      .btn-primary:hover {
        transform: translateY(-1.5px);
        filter: brightness(0.96);
      }
      .btn-ghost {
        background: transparent;
        color: var(--fg);
        border-color: rgba(26,26,26,0.15);
      }
      .btn-ghost:hover {
        background: rgba(26,26,26,0.02);
        border-color: rgba(26,26,26,0.28);
        transform: translateY(-1.5px);
      }
      .btn:active {
        transform: translateY(0);
        box-shadow: none;
      }

      /* Hero */
      .hero {
        display: grid;
        gap: 2rem;
        align-items: center;
        padding-block: 3rem;
      }
      .hero__content {
        display: grid; gap: 1rem;
      }
      .eyebrow {
        display: inline-flex; align-items: center; gap: 0.5rem;
        font-size: 0.875rem; color: var(--fg);
        background: var(--surface);
        border: 1px solid rgba(26,26,26,0.08);
        border-radius: 999px;
        padding: 0.375rem 0.75rem;
        width: fit-content;
        box-shadow: var(--shadow-sm);
      }
      .eyebrow .dot {
        width: 6px; height: 6px; border-radius: 999px; background: var(--accent);
        box-shadow: 0 0 0 3px rgba(199,162,90,0.18);
      }
      .hero h1 {
        font-size: clamp(2rem, 5vw, 3rem);
        line-height: 1.1;
      }
      .subtle {
        font-size: 1rem;
        color: var(--muted);
        max-width: 60ch;
      }
      .hero__actions {
        display: flex; flex-wrap: wrap; gap: 0.75rem;
        margin-top: 0.75rem;
      }

      /* Showcase card (optional visual without external assets) */
      .card {
        background: var(--surface);
        border: 1px solid rgba(26,26,26,0.08);
        border-radius: calc(var(--radius) + 4px);
        padding: 1.25rem;
        display: grid; gap: 0.875rem;
        box-shadow: var(--shadow-md);
      }
      .swatch-row {
        display: flex; align-items: center; gap: 0.5rem;
      }
      .swatch {
        width: 36px; height: 36px; border-radius: 8px; border: 1px solid rgba(26,26,26,0.08);
      }
      .swatch.ivory { background: var(--bg); }
      .swatch.charcoal { background: var(--fg); }
      .swatch.gold { background: var(--accent); }

      .features {
        display: grid; gap: 0.75rem;
      }
      .feature {
        display: flex; align-items: center; gap: 0.5rem;
        font-size: 0.95rem; color: var(--fg);
      }
      .tick {
        width: 18px; height: 18px; flex: 0 0 auto;
        border-radius: 50%;
        display: grid; place-items: center;
        background: rgba(199,162,90,0.22);
        color: #1a1a1a;
        border: 1px solid rgba(199,162,90,0.5);
        font-size: 0.75rem;
        line-height: 1;
      }

      /* Footer */
      footer {
        border-top: 1px solid rgba(0,0,0,0.05);
        background: rgba(250, 247, 242, 0.7);
      }
      .foot {
        display: flex; align-items: center; justify-content: space-between;
        gap: 1rem; padding-block: 1rem;
        font-size: 0.9rem;
        color: var(--muted);
      }

      /* Responsive */
      @media (min-width: 768px) {
        .hero {
          grid-template-columns: 1.1fr 0.9fr;
          gap: 2.5rem;
          padding-block: 4.5rem;
        }
        .card { padding: 1.5rem; }
      }

      @media (prefers-reduced-motion: reduce) {
        * { transition: none !important; }
        .btn:hover { transform: none; }
      }
    </style>
  </head>

  <body>
    <a href="#main" class="sr-only">Skip to content</a>

    <div class="wrapper">
      <header>
        <div class="container nav">
          <a href="/" class="brand" aria-label="Premium Bags Home">
            <span class="badge" aria-hidden="true"></span>
            <span>Premium&nbsp;Bags</span>
          </a>
          <nav class="nav-actions" aria-label="Primary actions">
             Replace hrefs with your real routes 
            <a class="btn btn-ghost" href="/users/login" aria-label="Log in to your account">Log In</a>
            <a class="btn btn-primary" href="/users/signup" aria-label="Create a new account">Sign Up</a>
          </nav>
        </div>
      </header>

      <main id="main">
        <section class="container hero" aria-labelledby="intro-title">
          <div class="hero__content">
            <span class="eyebrow"><span class="dot" aria-hidden="true"></span> Timeless Leather Craft</span>
            <h1 id="intro-title">Bags that carry more than essentials—carry your story.</h1>
            <p class="subtle">
              Discover premium leather bags designed to age beautifully. Built for everyday elegance,
              refined for a lifetime of journeys.
            </p>
            <div class="hero__actions">
               Replace hrefs with your real routes 
              <a class="btn btn-primary" href="/users/signup">Create Account</a>
              <a class="btn btn-ghost" href="/users/login">I already have an account</a>
            </div>
          </div>

          <aside class="card" aria-label="Highlights">
            <h2 class="sr-only">Why choose us</h2>
            <div class="swatch-row" aria-hidden="true">
              <div class="swatch ivory" title="Ivory"></div>
              <div class="swatch charcoal" title="Charcoal"></div>
              <div class="swatch gold" title="Gold"></div>
            </div>
            <div class="features">
              <div class="feature">
                <span class="tick" aria-hidden="true">✓</span>
                Full‑grain leather, precision stitching
              </div>
              <div class="feature">
                <span class="tick" aria-hidden="true">✓</span>
                Lifetime support and care
              </div>
              <div class="feature">
                <span class="tick" aria-hidden="true">✓</span>
                Secure checkout and fast shipping
              </div>
            </div>
          </aside>
        </section>
      </main>

      <footer>
        <div class="container foot">
          <span>© <span id="year" aria-hidden="true"></span> Premium Bags</span>
          <span>Crafted with care • Est. 2025</span>
        </div>
      </footer>
    </div>

     Year fallback with HTML only (no JS) 
    <noscript></noscript>
  </body>
</html>
